# Core FastAPI and dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic[email]==2.5.0
pydantic-settings==2.1.0

# Security
# passlib 1.7.4 is incompatible with bcrypt >= 4.1 (raises on init); pin a working bcrypt.
passlib==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0
PyJWT==2.8.0
python-multipart==0.0.6

# Database
# sqlite3 is built-in with Python (local dev); Turso/libSQL for serverless prod.
libsql==0.1.11

# HTTP client
httpx==0.25.2
requests==2.31.0

# Blockchain (optional feature; imports are guarded so the app runs without it)
web3==6.15.1
eth-account==0.11.0

# Validation and serialization
email-validator==2.1.0

# Rate limiting
slowapi==0.1.9

# Development and testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# Logging and monitoring
structlog==23.2.0

# Environment management
python-dotenv==1.0.0

# Type hints
typing-extensions==4.8.0

# CORS and middleware
starlette==0.27.0

# Report generation
reportlab==4.0.4
qrcode[pil]==7.4.2
