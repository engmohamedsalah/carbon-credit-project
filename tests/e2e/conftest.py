# Pytest configuration for the CI e2e smoke tests.
# No shared fixtures are needed yet; this file marks tests/e2e as a pytest rootdir
# and is a place to add browser/server fixtures if the smoke suite grows.
